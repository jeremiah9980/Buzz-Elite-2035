const json=(data,status=200,headers={})=>new Response(JSON.stringify(data,null,2),{status,headers:{'content-type':'application/json; charset=utf-8',...headers}});const cors=env=>({'access-control-allow-origin':env.ALLOWED_ORIGIN||'*','access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'content-type,authorization'});const httpError=(status,message)=>Object.assign(new Error(message),{status});async function readJson(request){try{return await request.json()}catch(error){throw error instanceof SyntaxError?httpError(400,`Invalid JSON body: ${error.message}`):error}}async function readConfig(env){if(!env.BUZZ_DATA)return null;const raw=await env.BUZZ_DATA.get('integration-config');return raw?JSON.parse(raw):null}async function writeLog(env,entry){if(!env.BUZZ_DATA)return;const key=`sync-log:${Date.now()}`;await env.BUZZ_DATA.put(key,JSON.stringify(entry),{expirationTtl:60*60*24*30})}async function ncsSearch(){throw httpError(503,'NCS adapter is not configured. Add an authorized NCS data source or documented API client.')}async function ncsRoster(){throw httpError(503,'NCS roster adapter is not configured.')}async function ncsEvents(){throw httpError(503,'NCS tournament adapter is not configured.')}async function gameChangerStats(){throw httpError(503,'GameChanger adapter is not configured. Use an authorized export or approved integration.')}async function runScheduledSync(env){const config=await readConfig(env);if(!config?.sync?.enabled)return {skipped:true,reason:'Synchronization disabled or configuration missing'};const result={startedAt:new Date().toISOString(),events:[],stats:null};try{if(config.tournamentTeamIds?.length)result.events=await ncsEvents(config.tournamentTeamIds,env);if(config.gamechanger?.teamId)result.stats=await gameChangerStats(config.gamechanger,env);result.completedAt=new Date().toISOString();await writeLog(env,{type:'scheduled-sync',ok:true,...result});return result}catch(error){await writeLog(env,{type:'scheduled-sync',ok:false,error:error.message,at:new Date().toISOString()});throw error}}export default{async fetch(request,env){const headers=cors(env);if(request.method==='OPTIONS')return new Response(null,{status:204,headers});const url=new URL(request.url);try{if(url.pathname==='/api/health')return json({ok:true,service:'buzz-elite-integrations',adapter:url.searchParams.get('adapter')||'all',configured:{kv:!!env.BUZZ_DATA,ncs:false,gamechanger:false},timestamp:new Date().toISOString()},200,headers);if(url.pathname==='/api/config'&&request.method==='GET')return json(await readConfig(env)||{},200,headers);if(url.pathname==='/api/config'&&request.method==='POST'){if(!env.BUZZ_DATA)return json({error:'BUZZ_DATA KV binding is required'},503,headers);const config=await readJson(request);await env.BUZZ_DATA.put('integration-config',JSON.stringify(config));return json({ok:true},200,headers)}if(url.pathname==='/api/ncs/teams'&&request.method==='GET')return json(await ncsSearch(Object.fromEntries(url.searchParams),env),200,headers);let match=url.pathname.match(/^\/api\/ncs\/teams\/([^/]+)\/roster$/);if(match&&request.method==='GET')return json(await ncsRoster(decodeURIComponent(match[1]),env),200,headers);if(url.pathname==='/api/ncs/events/sync'&&request.method==='POST'){const body=await readJson(request);return json(await ncsEvents(body.teamIds||[],env),200,headers)}match=url.pathname.match(/^\/api\/ncs\/events\/([^/]+)\/sync$/);if(match&&request.method==='POST'){const events=await ncsEvents([],env,{eventId:decodeURIComponent(match[1])});return json(events[0]||{},200,headers)}if(url.pathname==='/api/gamechanger/sync'&&request.method==='POST'){const body=await readJson(request);return json(await gameChangerStats(body,env),200,headers)}if(url.pathname==='/api/sync/run'&&request.method==='POST')return json(await runScheduledSync(env),200,headers);return json({error:'Not found'},404,headers)}catch(error){const status=Number.isInteger(error?.status)?error.status:500;return json({error:error?.message||'Internal Server Error'},status,headers)}},async scheduled(controller,env,ctx){ctx.waitUntil(runScheduledSync(env))}};